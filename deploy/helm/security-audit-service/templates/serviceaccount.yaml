{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "security-audit-service.serviceAccountName" . }}
  labels:
    {{- include "security-audit-service.labels" . | nindent 4 }}
{{- end }}
